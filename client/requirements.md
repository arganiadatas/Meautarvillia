## Packages
lightweight-charts | High-performance financial charts (TradingView style)
lucide-react | Beautiful icons for the dashboard
date-fns | Date formatting for charts and news
framer-motion | Smooth animations for cards and transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
